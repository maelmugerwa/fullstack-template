{
  "name": "fullstack-interview-template",
  "version": "1.0.0",
  "description": "A fullstack interview template with React, TypeScript, Express, and Prisma",
  "author": "Your Name",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "db:setup": "npm run db:migrate --workspace=server && npm run db:seed --workspace=server",
    "db:studio": "npm run db:studio --workspace=server",
    "db:reset": "npm run db:reset --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  }
}